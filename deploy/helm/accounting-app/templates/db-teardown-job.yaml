{{- if .Values.dbTeardown.enabled -}}
apiVersion: batch/v1
kind: Job
metadata:
  name: {{ include "accounting-app.fullname" . }}-db-teardown
  labels:
    {{- include "accounting-app.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": pre-delete
    "helm.sh/hook-weight": "0"
    "helm.sh/hook-delete-policy": before-hook-creation,hook-succeeded
spec:
  backoffLimit: 1
  ttlSecondsAfterFinished: 300
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: db-teardown
          image: postgres:17-alpine
          command:
            - sh
            - -c
            - |
              echo "Dropping database $DB_NAME..."
              dropdb --if-exists "$DB_NAME"
              echo "Database $DB_NAME dropped"
          env:
            - name: PGHOST
              valueFrom:
                secretKeyRef:
                  name: {{ include "accounting-app.fullname" . }}
                  key: DB_HOST
            - name: PGPORT
              valueFrom:
                secretKeyRef:
                  name: {{ include "accounting-app.fullname" . }}
                  key: DB_PORT
            - name: PGUSER
              value: {{ .Values.env.DB_USER | quote }}
            - name: PGPASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ include "accounting-app.fullname" . }}
                  key: DB_PASSWORD
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 100m
              memory: 128Mi
{{- end }}
